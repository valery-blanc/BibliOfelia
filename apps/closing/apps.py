from django.apps import AppConfig


class ClosingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.closing"
    verbose_name = "Activités, animations et bouclement"
